// ==== getters =====
const getters = {
  order(state) {
    return state.order;
  },

  isOrderDetail(state) {
    return state.isOrderDetail;
  },

  orderDetail(state) {
    return state.orderDetail;
  },
};

export default getters;
